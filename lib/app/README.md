# App

This directory is required by the project FSD architecture.

Put app composition code here:

- router
- bootstrap
- theme
- root/global providers

App-layer bootstrap, router, and global providers should live here.
Do not add new app-layer code under legacy folders such as `lib/starter` or `lib/shared/router`.
